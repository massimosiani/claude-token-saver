name: tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  hook:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run hook fixture tests
        run: bash tests/run.sh
